#include <iostream>
#include <set>
using namespace std;

int main() {
    set<int> s;
    long double sum = 0;
    int n;
    string st;
    while (cin >> st >> n) {
        if (st == "delete") {

        }
        else 
    }
}